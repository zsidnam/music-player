import PropTypes from 'prop-types';
import { Box } from '@material-ui/core';

// track Number

// Title

// Album

// Artist

// Duration

const Track = () => {
    return <Box>I am a track</Box>;
};

Track.propTypes = {};

export default Track;
