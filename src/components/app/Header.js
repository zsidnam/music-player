import React from 'react';
import { Box } from '@material-ui/core';

import { HEADER_COLOR } from '../../styles/theme';

const Header = () => {
    return <Box height={100} bgcolor={HEADER_COLOR}></Box>;
};

export default Header;
